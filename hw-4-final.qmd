My three plots:

1.  time series of concentration of nitrogen and carbon vs depth (dual axis plot)

```{r include = FALSE, message = FALSE}
# loading libraries and fonts for plots
library(tidyverse)
library(showtext)
library(here)
library(patchwork)
library(sysfonts)
library(ggtern)
library(lubridate)


font_add_google(name = 'Josefin Sans', family = 'josefin')
font_add_google(name = 'Inter', family = 'inter')

showtext_auto()
```

```{r}
# scaling coefficient
coeff <- 10


# subsetting dataset to only have 80m below sea level
nutrient_clean_2 <- nutrient_clean %>% 
  filter(depth <= 80)
ggplot(nutrient_clean_2, aes(x = depth))+
  geom_line(aes(y = dissolved_n), color = "#999933")+
  geom_line(aes(y = dissolved_c/coeff), color = "#117733")+
  scale_y_continuous(name = 'Dissolved Nitrogen (µmol/kg)',
                     sec.axis = sec_axis(~.*coeff, name = 'Dissolved Carbon (µmol/kg)'))+
  labs(x = 'Depth (m)',
       title = 'Depth Profile Comparison of Dissolved Carbon and Nitrogen')+
  scale_x_reverse()+
  coord_flip()+
  theme(aspect.ratio = 2,
        axis.title.x = element_text(family = 'inter', size = 14),
        axis.title.y = element_text(family = 'inter', size = 14),
        plot.title = element_text(family = 'josefin', size = 18, hjust = .5))

```

```{r}
nutrient_clean %>% 
  ggplot()+
  geom_point(aes(y = depth, x = chlorophyll), color = '#8A9A5B', alpha = 0.75)+
  scale_y_reverse()
```
